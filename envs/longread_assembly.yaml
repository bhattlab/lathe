name: longread_assembly_test
channels:
  - bioconda
  - biobuilds
  - intel
  - conda-forge 
dependencies:
  - python>3.5
  - pilon
  - nanopolish=0.10.2
  - bwa
  - minimap2
  - canu=1.7.1
  - samtools
  - bcftools
  - bedtools #used with tombo
  - bedops #used with tombo
  - biopython
  - pip
  - jellyfish
  - r-vegan 
  - r-ggplot2
  - r-plyr
  - tabix
  - ncurses=6.1
  - r-factoextra
  - tensorflow #makes medaka work
  - pip:
      - snakemake
      - git+https://github.com/elimoss/circlator.git
      - git+https://github.com/nanoporetech/tombo.git
      #- git+https://github.com/nanoporetech/medaka.git #this doesn't work.  you'll need to clone the repo and pip install . from there.

#versions are pinned only to force the newest one.  these will need to be updated manually.

#  - ont-tombo #tombo is installed in its own environment
# quickmerge must be installed manually
